pipeline {
    agent any
    stages {
    #stage('Build da imagem Docker Backend') {
          #steps {
                #script {
                    #sh 'docker build -t devops/backend -f backend/Dockerfile.backend ./backend'
                #}
            #}
        #}

        stage('Build da imagem Docker Frontend') {
            steps {
                script {
                    sh 'docker build -t devops/frontend -f frontend/Dockerfile.frontend ./frontend'
                }
            }
        }

        // Adicione outras etapas conforme necessário
    }
}

