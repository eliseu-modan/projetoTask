pipeline {
    agent any
    stages {

        stage('Build da imagem Docker Frontend') {
            steps {
                script {
                       sh 'docker build -t devops/app ./Dockerfile.frontend .'
                }
            }
        }

    }
}
