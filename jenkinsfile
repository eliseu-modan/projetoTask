pipeline {
    agent any
    stages {

        stage('Build da imagem Docker Frontend') {
            steps {
                script {
                    docker build -t devops/app -f Dockerfile.frontend .
                }
            }
        }

    }
}
