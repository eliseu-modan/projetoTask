pipeline {
    agent any
    stages {

        stage('Build da imagem Docker Frontend') {
            steps {
                script {
                  '  docker buildx build -t frontend -f Dockerfile.frontend .'
            
}
}
        }

    }
}
